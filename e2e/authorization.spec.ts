import { test } from "@playwright/test";

test.skip("a user cannot access another project's job", async () => {
  // Implement after Engineers 1 and 2 confirm ownership and authentication contracts.
});
